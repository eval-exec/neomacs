# Avoid rasterizing an output-sized desktop pattern before Weston permits its
# first repaint. Weston 15 uses a single-pixel buffer for a solid background.
# Presentation-sensitive scenarios must still wait for compositor feedback.
[shell]
background-color=0xff202020
