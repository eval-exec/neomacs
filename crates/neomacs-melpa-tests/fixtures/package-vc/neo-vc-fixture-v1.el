;;; neo-vc-fixture.el --- Offline package-vc fixture  -*- lexical-binding: t; -*-

;; Version: 1.0
;; Package-Requires: ((emacs "24.1"))

;;; Commentary:

;; First revision of the package-vc lifecycle fixture.

;;; Code:

;;;###autoload
(defun neo-vc-fixture-version ()
  "Return the installed package-vc fixture revision."
  "v1")

(provide 'neo-vc-fixture)

;;; neo-vc-fixture.el ends here
